#include <stdio.h>

void main(){
    int A, B, X;

    //recive the numbers
    scanf("%d", &A);
    scanf("%d", &B);

    //make count
    X = A + B;

    //Show in terminal the result of X
    printf("X = %d", X);

    return;
}